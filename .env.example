# ============================================
# 实验室数据分析系统 - Docker 环境配置
# ============================================
# 注意：这是模板文件，不含敏感信息
# 实际配置请复制到 .env.local 并修改
# ============================================

# 部署数据目录
DEPLOY_DIR=./deploy

# ============================================
# MySQL 配置
# ============================================
MYSQL_VERSION=8.0
MYSQL_PORT=3307
MYSQL_ROOT_PASSWORD=your_root_password_here
MYSQL_DATABASE=lumei
MYSQL_USER=your_db_user
MYSQL_PASSWORD=your_db_password
MYSQL_TIMEZONE=Asia/Shanghai

# ============================================
# Redis 配置
# ============================================
REDIS_VERSION=8.0-alpine
REDIS_PORT=6380
REDIS_PASSWORD=your_redis_password

# ============================================
# Qdrant 配置
# ============================================
QDRANT_VERSION=v1.12.1
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334

# ============================================
# TEI (Text Embeddings Inference) 配置
# ============================================
TEI_VERSION=1.6
TEI_PORT=8081
TEI_MODEL_PATH=/opt/models/bge-m3
TEI_MAX_BATCH_SIZE=32

# ============================================
# vLLM (大模型推理引擎) 配置
# ============================================
VLLM_VERSION=latest
VLLM_PORT=8082
VLLM_MODEL_PATH=/opt/models/qwen2.5-7b
VLLM_GPU_MEMORY_UTILIZATION=0.7
VLLM_MAX_MODEL_LEN=4096

# ============================================
# 网络配置
# ============================================
NETWORK_NAME=lm-network

# ============================================
# 容器前缀配置
# ============================================
CONTAINER_PREFIX=lm

# ============================================
# API 配置
# ============================================
API_PORT=9530

# ============================================
# 应用版本配置
# ============================================
# 从 VERSION 文件读取，或在此指定
# APP_VERSION=1.0.0

# ============================================
# 自动构建配置
# ============================================
# 是否在提交代码时自动构建 Docker 镜像
# 设置为 true 时，每次提交代码（如果 VERSION 文件变更）会自动触发构建
# 默认为 false，每台机器可独立配置
AUTO_BUILD=false
