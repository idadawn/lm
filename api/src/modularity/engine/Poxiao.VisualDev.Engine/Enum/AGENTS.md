<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-04-30 -->

# Enum

## Purpose
引擎枚举命名空间根。当前仅有 `VisualDevModelData/` 子目录，承载与可视化开发数据缓存类型相关的枚举（dictionary / static / dynamic / list 等）。

## Subdirectories
| Directory | Purpose |
|-----------|---------|
| `VisualDevModelData/` | 控件数据缓存类型枚举（`vModelType`） |

## For AI Agents

### Working in this directory
- 枚举值会通过 `[Description("...")]` 与前端约定的字符串保持一致，新增/重命名时必须同步前端代码。

<!-- MANUAL: Add manual notes below this line; they are preserved on regeneration -->
