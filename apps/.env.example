# ============================================
# 应用环境变量模板（应用服务器使用）
# 使用方法：复制为 .env 并修改配置
# ============================================

# 部署目录（相对于 docker-compose.yml 的位置）
DEPLOY_DIR=./deploy

# ============================================
# 端口配置
# ============================================
# API 服务端口
API_PORT=9530

# Nginx 对外端口（访问入口）
NGINX_PORT=8923

# ============================================
# 容器配置
# ============================================
# 容器名称前缀
CONTAINER_PREFIX=lm

# Nginx 版本
NGINX_VERSION=1.27-alpine

# 网络名称
NETWORK_NAME=lm-apps-network

# ============================================
# 基础环境连接配置（必须配置）
# ============================================
# 基础环境服务器地址（运行 MySQL/Redis/Qdrant 的服务器）
INFRA_HOST=10.0.0.5

# 基础环境端口
INFRA_MYSQL_PORT=3307
INFRA_REDIS_PORT=6380
INFRA_QDRANT_PORT=6333
INFRA_TEI_PORT=8081
INFRA_VLLM_PORT=8082

# ============================================
# 镜像仓库配置（可选）
# ============================================
# 镜像仓库地址，用于推送/拉取镜像
# REGISTRY=registry.cn-hangzhou.aliyuncs.com/your-namespace
