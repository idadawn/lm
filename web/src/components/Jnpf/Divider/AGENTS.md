<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-04-30 -->

# Divider

## Purpose
`JnpfDivider` 分隔线包装目录，封装 `a-divider`，提供表单中带文字的分隔行（默认 `left` 对齐）。

## Key Files
| File | Description |
|------|-------------|
| `index.ts` | barrel：导出 `JnpfDivider` |

## Subdirectories
| Directory | Purpose |
|-----------|---------|
| `src/` | SFC 实现（见 `src/AGENTS.md`） |

## For AI Agents

### Working in this directory
- 仅维护 barrel；分隔线视觉规范由内层 `Divider.vue` 决定。

## Dependencies
### Internal
- `/@/utils` — `withInstall`

<!-- MANUAL: Add manual notes below this line; they are preserved on regeneration -->
