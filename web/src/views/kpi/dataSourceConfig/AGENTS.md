<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-04-30 -->

# dataSourceConfig

## Purpose
KPI 数据源配置页面（占位）：当前仅有 `page-content-wrapper` + 「search box」「content」字符的脚手架壳，等待按需求接入数据源选择/连接测试逻辑。`defineOptions({ name: 'config' })`。

## Key Files
| File | Description |
|------|-------------|
| `index.vue` | 占位骨架，`reactive` 空对象，`<style lang="less" scoped>` 空块。 |

## For AI Agents

### Working in this directory
- 这是占位页：扩展时优先复用 `/@/components/Table` + `/@/api/systemData/dataSource`，参考 `kpi/dimension/index.vue` 的左树+表布局。
- 改名建议：`defineOptions({ name: 'kpi-dataSourceConfig' })` 以避免与其它 `config` 命名冲突。

## Dependencies
### Internal
- 暂无（待补充）

<!-- MANUAL: Add manual notes below this line; they are preserved on regeneration -->
